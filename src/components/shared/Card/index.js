import { Card as CardComponent, CardHeader, CardIcon, CardBody, CardFooter } from './Card'

CardComponent.Header = CardHeader
CardComponent.Icon = CardIcon
CardComponent.Body = CardBody
CardComponent.Footer = CardFooter

export default CardComponent
export { CardComponent as Card, CardHeader, CardIcon, CardBody, CardFooter }
